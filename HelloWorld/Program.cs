// This file is kept for backward compatibility and demonstration purposes.
// The HelloWorld project is now a class library - run the HelloWorld.Api project
// to see the greeting service in action via REST API endpoints.
//
// To run the API:
//   cd HelloWorld.Api
//   dotnet run
//
// Then navigate to:
//   - GET /api/greeting - Returns "Hello, World!"
//   - GET /api/greeting/{name} - Returns "Hello, {name}!"

// Example of using the GreetingService directly:
// var service = new HelloWorld.GreetingService();
// Console.WriteLine(service.GetGreeting());
// Console.WriteLine(service.GetGreeting("Developer"));
